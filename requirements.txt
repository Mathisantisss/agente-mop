# Agente MOP — Dependencias para Streamlit Cloud
# Versión optimizada para Free Tier (1 GB RAM)

# Framework web
streamlit==1.57.0

# Anthropic API
anthropic==0.100.0

# Base vectorial
chromadb==1.5.9

# ⚠️ Embeddings con fastembed (ONNX, ~150 MB) en lugar de
# sentence-transformers + PyTorch (~700 MB). Misma calidad,
# mucho menos memoria — esencial para Streamlit Cloud Free Tier.
# ⚠️ Pineamos fastembed==0.5.1 — versiones >=0.6 cambiaron el pooling
# (mean pooling) y producirian embeddings INCOMPATIBLES con los que
# tenemos en ChromaDB (indexados con sentence-transformers).
fastembed==0.5.1
onnxruntime>=1.18.0

# Fix protobuf para chromadb + opentelemetry
protobuf>=4.25.3,<5.0.0

# Generación de documentos
openpyxl==3.1.5
python-docx==1.2.0
reportlab==4.5.1

# Utilidades
python-dotenv==1.2.2
rich>=13.0.0
