# Agente MOP — Dependencias para Streamlit Cloud
# Pinned a versiones probadas en desarrollo

# Framework web
streamlit==1.57.0

# Anthropic API
anthropic==0.100.0

# Base vectorial y embeddings
chromadb==1.5.9
sentence-transformers==5.4.1

# ⚠️ Fix obligatorio para que chromadb funcione en Streamlit Cloud:
# protobuf 5.x rompe los _pb2 generados por opentelemetry (dependencia de chromadb).
# Pinear a 4.x es el fix oficial recomendado por la comunidad chromadb.
protobuf>=4.25.3,<5.0.0

# Generación de documentos
openpyxl==3.1.5
python-docx==1.2.0
reportlab==4.5.1

# Utilidades
python-dotenv==1.2.2
rich>=13.0.0
